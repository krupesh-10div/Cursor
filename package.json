{
  "name": "custom-accordion-blocks",
  "version": "1.1.0",
  "private": true,
  "description": "Readable source for adr/custom-accordion, adr/accordion, and gutenreact/webkul-cards blocks",
  "scripts": {
    "start": "wp-scripts start",
    "build": "wp-scripts build && npm run postbuild",
    "build:theme": "WP_BUILD_DIR=accordion-blocks WP_BUILD_BASENAME=block.build.js npm run build",
    "postbuild": "node scripts/copy-to-theme.js",
    "lint": "wp-scripts lint-js"
  },
  "dependencies": {
    "lodash.times": "^4.3.2",
    "memize": "^1.1.0"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.23.0",
    "webpack": "^5.101.3",
    "webpack-cli": "^5.1.4"
  }
}
